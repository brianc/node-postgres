{
  "name": "pg-protocol",
  "version": "1.5.0",
  "description": "The postgres client/server binary protocol, implemented in TypeScript",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "license": "MIT",
  "devDependencies": {
    "@types/mocha": "^5.2.7",
    "@types/node": "^12.12.21",
    "chunky": "^0.0.0",
    "mocha": "^7.1.2",
    "nyc": "15.1.0",
    "ts-node": "^8.5.4",
    "typescript": "^4.0.3"
  },
  "scripts": {
    "build": "tsc --build",
    "build:watch": "tsc --build --watch",
    "test": "nyc mocha -r ts-node/register test/**/*.test.ts",
    "test:watch": "nyc mocha -r -w ts-node/register test/**/*.test.ts"
  },
  "repository": {
    "type": "git",
    "url": "git://github.com/brianc/node-postgres.git",
    "directory": "packages/pg-protocol"
  },
  "files": [
    "/dist/*{js,ts,map}",
    "/src"
  ]
}
